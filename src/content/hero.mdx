---
section: hero
name: Marlow Fernandez
tagline: Software Engineering Leader
---

This body is not rendered. `hero.mdx` exists so the Hero's name and tagline
come through the same frontmatter contract as every other section, rather than
being hardcoded in a component.

The tagline is the resume's own subtitle, and `mockups.md`'s desktop layout
shows exactly these two lines under the `h1`.
