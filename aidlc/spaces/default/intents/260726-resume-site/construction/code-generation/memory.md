<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-07-31T01:00:00Z — real tension between this stage's generic test-volume guidance and this project's affirmed practice, surfaced to the human in the plan rather than silently resolved. `aidlc-state.md` records `Test Strategy: Standard`, and the stage file maps Standard to "unit test files per component (5-8 tests each) + integration test stubs." But `team.md`'s Testing Posture — promoted from an affirmed Practices Discovery answer (Q3) — explicitly dropped numeric volume/coverage targets as a poor fit for a content site with near-zero business logic, replacing them with a named check set: build succeeds, links resolve, a11y/perf budgets pass, theme-toggle behavior check, content-schema validation. team.md is the narrower, human-affirmed layer and it names specific checks rather than a per-component count, so the plan proposes tests matching those named checks plus component smoke tests, NOT a mechanical 5-8-per-component quota. Flagged in the plan so the human can overrule if they actually want the higher volume
- 2026-07-31T01:00:00Z — no git worktree created despite `workspace_requires: true` and org.md's worktree language. `team.md`'s Way of Working (affirmed at Practices Discovery Q1) is direct commits to `main` with no branches; a worktree exists to isolate a branch, so creating one would contradict the affirmed practice for no benefit on a single-builder project

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-08-01T22:00:00Z — unit 1 generated; independently re-ran build/test/lint rather than accepting the developer agent's report (build PASS with static export, 60/60 tests PASS, lint clean) and verified the one contradiction it self-reported: `out/404.html` genuinely exists at 11,910 bytes, which contradicts refined-mockups Q1's confirmed "no custom 404 page — a broken/mistyped URL shows GitHub Pages' default 404." Next's static export always emits it and GitHub Pages prefers it over its own. Left in place and carried to the gate as a real decision rather than silently deleted or silently accepted
- 2026-08-01T22:00:00Z — developer agent surfaced two CSP gaps beyond the ones security-requirements.md already documents (script-src needs 'unsafe-inline' because 12 inline `__next_f.push` scripts exist and CSP3 makes a partial hash list break the page; the meta tag can't be first in head because Next hoists its stylesheet and 5 script tags above it). Recorded in code-summary.md as a documentation gap in security-requirements.md rather than quietly leaving the artifact overstating the posture — same class of over-claim this stage's own NFR predecessor was reviewed three times for
- 2026-08-01T22:00:00Z — a genuine conflict between governing documents, resolved toward the narrower one and recorded rather than hidden: construction.md says "silent failures are not acceptable," but unit-of-work.md and component-methods.md both explicitly specify silent localStorage fallback/no-op for ThemeToggle. Followed the unit-level spec, with each catch annotated with what it absorbs

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-08-02T00:00:00Z — my own dispatch prompt contradicted the plan it was dispatching. code-generation-plan.md, unit-of-work.md, and security-requirements.md all say content must NOT be re-derived from the resume PDF; my Task prompt to the developer agent then explicitly authorized reading the PDF "for a detail requirements.md summarizes rather than quotes." The agent correctly followed the dispatch instruction, and the reviewer correctly flagged the output as violating the written rule. The lesson is narrow and worth keeping: when dispatching an agent against a plan, the prompt must not quietly widen a constraint the plan states — if the constraint is wrong, amend the plan and re-approve, don't carve an exception in the brief where no reviewer will see it
- 2026-08-02T00:00:00Z — the substantive question underneath that error was worth the reviewer's catch regardless: HII's shipped bullets name specific naval training facilities (SCTTR, TTGP) and describe classified-environment security architecture, which is materially more specific than the "Navy/DHS clients + Secret Clearance" granularity requirements.md Q4/Q5 actually asked about. Surfaced it to the human as a real question rather than assuming the broader Q4/Q5 answers covered it — confirmed "publish as-is" at that specific granularity. This is the same practice learned at requirements-analysis (ask before sign-off, don't journal after) applied one layer deeper

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
