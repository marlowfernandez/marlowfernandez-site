<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-07-29T22:40:00Z — `consumes_absent` listed both functional-design artifacts (business-logic-model.md, business-rules.md) with `expected: true` — absent by design because `functional-design` is SKIP for this composed scope. Applied the stage's own documented fallback (Step 2: "when its artifacts are absent, derive the NFR context from the requirements... never invent the content of a missing artifact") and derived everything from `requirements.md` plus the Inception design artifacts instead. Did not fabricate business-logic content for a content site that genuinely has none
- 2026-07-29T22:40:00Z — the engine narrowed `produces` from 5 artifacts to 3 for this unit (dropped scalability-requirements and reliability-requirements) via `produces_kinds`: both units are `kind: ui`, and those two artifacts are declared `[service]`-only. Honored the engine-resolved produces list rather than generating all 5 from the stage file's unfiltered frontmatter
- 2026-07-29T22:40:00Z — NFR budgets (performance, accessibility, security posture) are site-wide facts, not unit-scoped ones — a Lighthouse score or a CSP applies to the deployed site as a whole. Asked the human once during this first unit's pass and will apply the same confirmed answers to the second unit rather than re-interviewing, since re-asking identical questions per unit would be duplicate questioning of the kind project.md's learned practice warns against

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-07-30T14:55:00Z — reviewer iteration 1 (unit site-shell-walking-skeleton) returned NOT-READY on two findings, both valid but one requiring verification before acting: it reported that `accessibility-checklist.md` "does not exist anywhere," recommending I reword the claim to say manual AA verification is unowned. Checked directly with `find` before editing — the file DOES exist at `inception/refined-mockups/accessibility-checklist.md`; the reviewer's read scope was correctly bounded to this unit + exempt paths, so its non-existence conclusion was scope-limited, not correct. The underlying defect it identified was real though (the citation had no path/stage attribution, unlike every other reference in the doc), so I fixed the actual defect by adding the path + producing stage rather than adopting the recommended reword, which would have asserted something false. Also added a genuinely new disclosure while there: the manual AA items have no automated enforcement anywhere (performance-validation is SKIP, CI only runs the Lighthouse subset). Second finding (tech-stack-decisions.md not acknowledging the absent functional-design inputs like its two siblings did) was straightforwardly correct and fixed as recommended

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-07-30T15:05:00Z — reviewer iteration 2 caught that my own iteration-1 fix introduced a fresh unverified claim: while correcting an unattributed citation I added "those manual items have no automated enforcement anywhere in this workflow," which contradicts this same stage's tech-stack-decisions.md documenting an automated theme-toggle check covering focus visibility and contrast — two items that ARE in accessibility-checklist.md. Pattern worth noting: I overcorrected from "understated the gap" straight past accurate into "overstated the gap," in the same sentence I was fixing. Narrowed the claim to split what genuinely has automated coverage (focus/contrast via theme-toggle check) from what genuinely doesn't (tab order, screen-reader announcements, landmarks, zoom, touch targets), rather than asserting a blanket either/or in either direction

- 2026-07-30T15:15:00Z — reviewer iteration 3 caught a third successive error in the SAME sentence: I claimed the theme-toggle check covers accessibility-checklist.md's "visible focus indicator on every focusable element," but that item's scope is explicitly page-wide (every link too) while the check only asserts the toggle's own focus ring. Fixed by splitting that single checklist item across both lists — toggle's focus ring covered, page-wide focus indicator NOT covered. Pattern worth naming: across three iterations on one sentence I went understated -> overstated -> overstated-differently, each time because I asserted a coverage relationship without checking the *scope* of the item being covered, only its topic. Topic overlap is not coverage. `reviewer_max_iterations` is 2 and this was iteration 3, so per §12a ("If READY or iterations exhausted: proceed") the fix was applied without a fourth dispatch; the engine accepts a NOT-READY-after-cap receipt, and the fix is a one-clause correction verifiable against the two quoted source lines rather than something needing fresh adversarial review

- 2026-07-30T16:00:00Z — reviewer iteration 1 on unit `content-sections` returned NOT-READY on one citation defect: performance-requirements.md's closing line said "the inherited budgets trace to this stage's confirmed Q1/Q2/Q4," but this unit has no questions file — those Qs belong to the shell unit's pass. The same file got it right two paragraphs earlier and dropped the unit qualifier only in the closing sentence. Fixed by attributing to the shell unit explicitly. The reviewer also raised a fair non-blocking housekeeping point: the shell unit's artifact still carried a NOT-READY as its terminal verdict despite the fix having been applied post-cap, which would read to a future reader as an unresolved dependency — added an explicit closure note to that artifact recording that the fix landed, without overwriting the genuine reviewer verdict (the audit trail legitimately carries NOT-READY-after-cap, and rewriting the verdict to look approved would be falsifying the record)

- 2026-07-30T16:10:00Z — the content-sections reviewer flagged an arithmetic tension in my post-cap closure note; checking stage-protocol.md §12a step 3 settled it AGAINST me. The rule is `reviewIterations < reviewer_max_iterations (default 2)`, so after iteration 2's NOT-READY the loop was already exhausted and the protocol's instruction was to proceed to the gate with unresolved findings noted — iteration 3 on the shell unit should never have been dispatched. My closure note had justified declining a *fourth* iteration while never acknowledging the third had already overrun; that was a plausible-sounding rationalization, not correct reasoning, and it took an external reviewer noticing the off-by-one for me to actually go read the rule. Corrected the note to state the overrun plainly. Practical consequence: this stage's approval gate must carry §12a's unresolved-findings status for the shell unit rather than presenting as clean, since its terminal reviewer verdict is NOT-READY

- 2026-07-30T16:40:00Z — the engine refused the approval: post-cap edits to the shell unit's performance-requirements.md invalidated its review receipt, since the completion guard only honors receipts recorded after the latest produces[] write. Correct enforcement — an unreviewed edit to a design artifact shouldn't clear a gate. Dispatched a fresh coverage review, which then found my "iteration 3 exceeded the cap" correction was ITSELF wrong: reading §12a's check-then-increment ordering with the counter tracking retries, iteration 3 was licensed. Read the protocol text directly rather than accept that on faith, and it is genuinely ambiguous — `reviewIterations` is never defined as starting at 0 or 1, nor as counting passes vs retries. Net pattern across this stage, worth naming honestly: I asserted one reading confidently, was told it was wrong, asserted the opposite just as confidently, and was told THAT was wrong. Both times the confident assertion outran the text. Settled by recording the ambiguity as ambiguous, and refocusing the note on the part that isn't: §12a's exhausted branch says proceed to the gate, and I edited the artifact instead — true regardless of how iterations are counted

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
